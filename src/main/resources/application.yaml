server:
  port: 8081
  servlet:
    context-path: /task

spring:
  liquibase:
    enabled: false
    liquibase-schema: public
    change-log: classpath:db/changelog/db.changelog-master.yaml
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: PandoraSQ123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL10Dialect
        hbm2ddl:
          auto: none
        default_schema: public
  security:
    user:
      name: admin
      password: admin
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8083/auth/realms/sqgolliaf
          jwk-set-uri: http://localhost:8083/auth/realms/sqgolliaf/protocol/openid-connect/certs

springfox:
  documentation:
    swagger-ui:
      base-url: /swagger-ui
